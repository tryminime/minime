apiVersion: v1
kind: Service
metadata:
  name: minime-frontend
  namespace: minime
  labels:
    app: minime-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: minime-frontend
