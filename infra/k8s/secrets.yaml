apiVersion: v1
kind: Secret
metadata:
  name: minime-secrets
  namespace: minime
type: Opaque
stringData:
  STRIPE_PUBLISHABLE_KEY: "pk_live_XXXXXXXXXXXXX"  # Replace with actual key
  GITHUB_CLIENT_ID: "XXXXXXXXXXXXX"  # Replace with actual OAuth client ID
  GOOGLE_CLIENT_ID: "XXXXXXXXXXXXX"  # Replace with actual OAuth client ID
  # Add other secrets as needed
